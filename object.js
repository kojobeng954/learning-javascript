let info = {
    firstName: "Ernest",
    age: 20,
    country: "Ghana"
}

function logData() {
    console.log(info.firstName + " is " + info.age + " years old and lives in " + info.country + ".")
}

logData();